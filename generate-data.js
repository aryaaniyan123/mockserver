var faker = require('faker');

/*function generateData () {
  var messages = [];
  for (var id = 0; id < 10; id++) {
    
    messages.push({
      "id": id,
      "date_sent": date,
      "priority": priority,
      "message": message,
      "status": status
    });
  }

  return {messages};
}

module.exports = generateData;*/